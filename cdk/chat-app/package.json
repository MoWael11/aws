{
  "name": "infrastructure",
  "version": "0.1.0",
  "description": "Infrastructure",
  "scripts": {
    "install:all": "npm run install:backend && npm run install:frontend && npm run install:infrastructure",
    "install:infrastructure": "cd infrastructure && npm install && cd -",
    "install:backend": "cd backend && npm run install:all && cd -",
    "install:frontend": "cd frontend && npm install && cd -",
    "build:frontend": "cd frontend && npm run build && cd -",
    "build:backend": "cd backend && npm run build:all && cd -",
    "deploy:infra": "cd infrastructure && npm run deploy && cd -",
    "deploy:frontend": "npm run --prefix frontend deploy --"
  },
  "devDependencies": {
    "@types/jest": "^30",
    "@types/node": "^24.10.1",
    "aws-cdk": "2.1126.0",
    "esbuild": "^0.28.1",
    "jest": "^30",
    "ts-jest": "^29",
    "ts-node": "^10.9.2",
    "typescript": "~5.9.3"
  }
}